# 开发者快速开始指南

## 前置准备

在开始之前,请确保您的系统已安装以下工具:

### 1. 安装 Node.js

访问 [Node.js 官网](https://nodejs.org/) 下载并安装 LTS 版本 (推荐 v18 或更高版本)

验证安装:
```bash
node --version  # 应显示 v16.x.x 或更高
npm --version   # 应显示 8.x.x 或更高
```

### 2. 安装 pnpm (推荐)

```bash
npm install -g pnpm
```

验证安装:
```bash
pnpm --version  # 应显示 8.x.x 或更高
```

## 项目启动步骤

### 1. 克隆项目

```bash
git clone <repository-url>
cd st
```

### 2. 安装依赖

```bash
pnpm install
```

如果遇到网络问题,可以使用国内镜像:
```bash
pnpm config set registry https://registry.npmmirror.com
pnpm install
```

### 3. 启动开发服务器

有两种方式启动项目:

#### 方式一:仅 Web 模式 (推荐用于界面开发)

```bash
pnpm run dev
```

然后在浏览器中访问: http://localhost:5173

优点:
- 启动快速
- 热更新流畅
- 便于调试

缺点:
- 无法使用 Electron 特定功能(文件操作、导入导出等)

#### 方式二:Electron 完整模式

```bash
pnpm run electron:dev
```

优点:
- 完整的桌面应用体验
- 可以测试所有功能

缺点:
- 启动较慢
- 首次启动需要下载 Electron

### 4. 代码检查和格式化

```bash
# 检查代码规范
pnpm run lint

# 自动格式化代码
pnpm run format
```

## 开发工作流

### 推荐的开发流程

1. **界面开发**: 使用 `pnpm run dev` 在浏览器中快速开发
2. **功能测试**: 使用 `pnpm run electron:dev` 测试完整功能
3. **代码提交前**: 运行 `pnpm run lint` 检查代码规范

### 热更新说明

- **Web 模式**: 修改代码后自动刷新
- **Electron 模式**: 
  - 渲染进程代码修改会自动刷新
  - 主进程代码修改需要手动重启

## 项目结构说明

```
st/
├── electron/           # Electron 主进程代码
│   ├── main.ts        # 窗口创建、IPC 通信
│   └── preload.ts     # 安全的 API 暴露
├── src/               # React 应用代码
│   ├── components/    # 可复用组件
│   ├── data/         # 静态数据
│   ├── pages/        # 页面组件
│   ├── store/        # Zustand 状态
│   ├── types/        # TypeScript 类型
│   └── App.tsx       # 应用入口
├── public/           # 静态资源
└── package.json      # 项目配置
```

## 常见开发场景

### 添加新页面

1. 在 `src/pages/` 创建新页面组件
2. 在 `src/App.tsx` 添加路由配置
3. 在 `src/components/layout/MainLayout.tsx` 添加菜单项

### 添加新课程

1. 在 `src/data/roadmap.ts` 添加课程节点
2. 在 `src/data/lessons.ts` 添加课程内容

### 修改主题样式

1. `src/index.css` - 全局样式
2. `tailwind.config.js` - Tailwind 配置
3. `src/App.tsx` - Ant Design 主题配置

### 添加状态管理

在 `src/store/` 创建新的 Zustand store:

```typescript
import { create } from 'zustand';

interface YourStore {
  // 定义状态和方法
}

export const useYourStore = create<YourStore>((set) => ({
  // 实现状态和方法
}));
```

## 调试技巧

### Web 模式调试

1. 使用浏览器开发者工具 (F12)
2. React DevTools 浏览器扩展
3. console.log() 调试

### Electron 模式调试

1. 主进程调试:
   ```bash
   # 在 electron/main.ts 中添加 debugger
   # 使用 VS Code 调试器
   ```

2. 渲染进程调试:
   - 应用会自动打开 DevTools
   - 或按 Ctrl+Shift+I (Windows) / Cmd+Option+I (Mac)

## 构建和打包

### 开发构建

```bash
pnpm run build
```

输出目录: `dist/`

### 生产打包

```bash
# Windows 安装包
pnpm run build:win

# 输出目录: release/
```

## 常见问题排查

### 问题 1: 依赖安装失败

```bash
# 清除缓存
pnpm store prune
rm -rf node_modules
pnpm install
```

### 问题 2: Electron 下载失败

```bash
# 使用国内镜像
export ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
pnpm install
```

### 问题 3: 端口已被占用

修改 `vite.config.ts`:
```typescript
server: {
  port: 5174, // 改为其他端口
},
```

### 问题 4: TypeScript 类型错误

```bash
# 重新生成类型声明
pnpm run build
```

## 性能优化建议

1. **代码分割**: 使用 React.lazy() 懒加载页面
2. **图片优化**: 使用适当格式和尺寸的图片
3. **依赖优化**: 避免导入整个库,使用按需导入
4. **打包优化**: 检查 bundle 大小,移除未使用的依赖

## Git 提交规范

推荐使用 Conventional Commits 规范:

```bash
feat: 添加新功能
fix: 修复 bug
docs: 文档更新
style: 代码格式调整
refactor: 代码重构
test: 测试相关
chore: 构建/工具链更新
```

示例:
```bash
git commit -m "feat: 添加代码编辑器组件"
git commit -m "fix: 修复学习进度保存问题"
```

## 获取帮助

- 查看文档: README.md, INSTALL.md, USER_GUIDE.md
- 提交 Issue: GitHub Issues
- 查看代码注释: 代码中有详细的注释说明

## 下一步

- [ ] 熟悉项目结构和代码组织
- [ ] 运行项目并浏览各个功能模块
- [ ] 阅读现有代码,理解实现思路
- [ ] 开始添加新功能或修复问题

祝开发愉快! 🚀
